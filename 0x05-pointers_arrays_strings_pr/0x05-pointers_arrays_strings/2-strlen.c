#include "main.h"
/**
 * _strlen - return the length of a string
 * @s: the string pointer
 * Return: the length number
 */
int _strlen(char *s)
{
	int a;

	for (a = 0; s[a]; a++)
	{
	}
	return (a);
}
