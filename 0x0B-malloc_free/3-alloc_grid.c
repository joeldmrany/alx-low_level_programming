#include "main.h"
#include <stdlib.h>

int **alloc_grid(int width, int height)
{
	int w, h;

	for (h = 0; h < height; h++)
	{
		for (w = 0; w < width; w++)
		{
			return (0);
		}
		_putchar('\n');
	}
	return (0);
}
