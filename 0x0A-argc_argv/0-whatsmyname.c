#include <stdio.h>
/**
 * main - that is the main task
 * @argc: the number of arguments
 * @argv: the argument array
 * Return: 0
 */
int main(int argc, char **argv)
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
